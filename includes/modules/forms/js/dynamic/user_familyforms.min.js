var user_family = new function(){document.addEventListener('DOMContentLoaded',function(){console.log('Dynamic user_family forms js loaded');FormFunctions.tidyMultiInputs();form=document.querySelector('[data-formid="12"]');form.querySelectorAll('select, input, textarea').forEach(el=>user_family.p(el));});var prevEl='';var q=function(event){var el=event.target;form=el.closest('form');var n=el.name;if(n==''||n==undefined){if(el.closest('.nice-select-dropdown')!=null&&el.closest('.inputwrapper')!=null){el.closest('.inputwrapper').querySelectorAll('select').forEach(select=>{if(el.dataset.value==select.value){el=select;n=select.name;}});}else{return;}}if(el==prevEl){return;}prevEl=el;setTimeout(function(){prevEl='';},100);if(n=='nextBtn'){FormFunctions.nextPrev(1);}else if(n=='prevBtn'){FormFunctions.nextPrev(-1);}user_family.p(el);};window.addEventListener('click',q);window.addEventListener('input',q);this.p=function(el){var n=el.name;if(n=='family[partner]'){var v_1=FormFunctions.getFieldValue('family[partner]',form,true,'',true);if(v_1!=''){form.querySelectorAll('[name="married_since_label"], [name="family[weddingdate]"], [name="family[picture]_files[]"], [name^="family[children]"]').forEach(el=>{try{if(!el.closest('.inputwrapper').matches('.action-processed')){el.closest('.inputwrapper').classList.add('action-processed');el.closest('.inputwrapper').classList.remove('hidden');}}catch(e){el.classList.remove('hidden');}});document.querySelectorAll('.action-processed').forEach(el=>{el.classList.remove('action-processed')});}if(v_1==''){form.querySelectorAll('[name="married_since_label"], [name="family[weddingdate]"], [name="family[picture]_files[]"], [name^="family[children]"]').forEach(el=>{try{if(!el.closest('.inputwrapper').matches('.action-processed')){el.closest('.inputwrapper').classList.add('action-processed');el.closest('.inputwrapper').classList.add('hidden');}}catch(e){el.classList.add('hidden');}});document.querySelectorAll('.action-processed').forEach(el=>{el.classList.remove('action-processed')});}}};};

async function submitAddAccountForm(event){
	var target		= event.target;

	var response	= await FormSubmit.submitForm(target, 'user_management/add_useraccount');

	if(response){
		var form		= target.closest('form');

		var firstName	= form.querySelector('[name="first_name"]').value;
		var lastName	= form.querySelector('[name="last_name"]').value;
		var userId		= response.user_id;

		//check if we should add a new child field
		var emptyFound	= false;
		document.querySelectorAll('select[name^="family"]').forEach(select=>{if(select.value==''){emptyFound=true}});

		if(!emptyFound){
			document.querySelector('select[name^="family"]').closest('form').querySelector('.add.button').click();
		}

		var opt 		= document.createElement('option');
		opt.value 		= userId;
		opt.innerHTML 	= firstName+' '+lastName;

		document.querySelectorAll('select[name^="family"]').forEach(select=>{
			select.appendChild(opt);

			// Make the new name selected if the there is no selection currently
			if(select.selectedIndex == 0){
				select.querySelector(`[value="${userId}"]`).defaultSelected	= true;
			}

			// Update the nice select
			select._niceselect.update();
		});

		Main.displayMessage(response.message, 'success');
	}

	Main.hideModals();
}

document.addEventListener("DOMContentLoaded",function() {
	document.querySelectorAll('[name="add_user_account_button"]').forEach(el=>el.addEventListener('click',function(){
		Main.showModal('add_account');
	}));

	document.querySelectorAll('[name="adduseraccount"]').forEach(el=>el.addEventListener('click', ev=>{
		
		ev.preventDefault();
		ev.stopPropagation();
		submitAddAccountForm(ev);
	}));
});